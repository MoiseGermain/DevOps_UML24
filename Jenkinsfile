pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                // Replace 'uname -a' with the echo command for the testing branch
                sh 'echo "This is a test branch"'
            }
        }
    }
}
